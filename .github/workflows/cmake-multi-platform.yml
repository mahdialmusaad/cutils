name: CMake on multiple platforms
on:
  pull_request:
  push:
    branches: [ main ]
  workflow_dispatch:
permissions:
  contents: read
  pull-requests: read
jobs:
  linux:
    name: Linux
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - name: Configure and build
      run: |
        cmake -B build -DCU_SETTING_QUIET=1
        cmake --build build
  macos:
    name: MacOS
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v5
    - name: Configure and build
      run: |
        cmake -B build -DCU_SETTING_QUIET=1
        cmake --build build
  windows:
    name: Windows
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v5
    - name: Configure and build
      run: |
        cmake -B build -A x64 -DCU_SETTING_QUIET=1
        cmake --build build